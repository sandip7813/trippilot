APP_NAME=TripPilot
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# MySQL (Docker: docker compose up -d)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3307
DB_DATABASE=trippilot
DB_USERNAME=trippilot
DB_PASSWORD=secret

# MongoDB (Docker: docker compose up -d)
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DATABASE=trippilot

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# External integrations (Phase 2+)
MAPS_DRIVER=geoapify
GEOAPIFY_API_KEY=
MAPS_DEFAULT_COUNTRY=in

WEATHER_DRIVER=open_meteo
# Open-Meteo is free and needs no API key for non-commercial use.
# OPENWEATHERMAP_API_KEY=

TRAIN_DRIVER=railradar
RAILRADAR_API_KEY=
# TRAIN_CACHE_TTL=43200

AI_DRIVER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_EMBEDDING_MODEL=text-embedding-004

TRIP_COVER_DRIVER=rotating
TRIP_COVER_ENABLED=true
UNSPLASH_ACCESS_KEY=
# Pollinations AI art (experimental — often inaccurate for specific cities)
# TRIP_COVER_DRIVER=pollinations
POLLINATIONS_IMAGE_MODEL=flux
POLLINATIONS_ENHANCE=false
TRIP_COVER_GEMINI_PROMPT=true
# Uses Gemini text model to build landmark-focused Unsplash search queries
# Optional: use Gemini for covers instead (requires billing/quota)
# TRIP_COVER_DRIVER=gemini
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
# GEMINI_IMAGE_ENABLED=true
# Optional size overrides (defaults: banner 1920x900, thumb 384x512)
# TRIP_COVER_BANNER_WIDTH=1920
# TRIP_COVER_BANNER_HEIGHT=900
# TRIP_COVER_THUMB_WIDTH=384
# TRIP_COVER_THUMB_HEIGHT=512

VITE_APP_NAME="${APP_NAME}"

# Brand: plane | compass | pin | globe | monogram
TRIPPILOT_LOGO=compass

# Docker Compose port overrides (optional)
# MySQL uses 3307 by default to avoid conflict with local MySQL on 3306
FORWARD_DB_PORT=3307
FORWARD_MONGODB_PORT=27017
